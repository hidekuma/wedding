{
  "name": "wedding-invitation",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://hidekuma.github.io/wedding",
  "dependencies": {
    "cra-template-pwa": "2.0.0",
    "framer-motion": "^10.16.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-intersection-observer": "^9.15.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "node convert-images.js && react-scripts start",
    "build": "node convert-images.js && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sharp": "^0.34.2"
  }
}
